{:api-version "networking.k8s.io/v1"
 :kind "Ingress"
 :metadata
 {:name ref/name}
 :spec
 {:rules
  {:host ref/host.name
   :http
   {:path ref/host.path
    :pathType: "Prefix"
    :backend
    {:service
     {:name ref/host.target
      :port
     {:number 80}}}}}}}
